<ul>
  <% if Admin::JobsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_jobs_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_job_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::JobsController.sortable? and Job.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Jobs"),
                 admin_jobs_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Jobs"),
                 admin_jobs_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>