<ul>
  <% if Admin::TopicsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_topics_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_topic_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::TopicsController.sortable? and Topic.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Topics"),
                 admin_topics_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Topics"),
                 admin_topics_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>